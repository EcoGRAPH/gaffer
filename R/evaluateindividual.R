evaluategeneration <- function(model=NULL,
                               modeldata=NULL,
                               ...) {

  return(bam(model=model,
             data=modeldata,
             ...))

}
